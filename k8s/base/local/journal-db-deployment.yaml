apiVersion: apps/v1
kind: Deployment
metadata:
  name: journal-db
  labels:
    app: journal-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: journal-db
  template:
    metadata:
      labels:
        app: journal-db
    spec:
      containers:
        - name: journal-db
          image: postgres:alpine
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: journal-db-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: journal-db-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: journal-db-config
                  key: POSTGRES_DB
          ports:
            - containerPort: 5433
          volumeMounts:
            - name: journal-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: journal-db-storage
          persistentVolumeClaim:
            claimName: journal-db-pvc